  Data Cleaner  
    
    .Start with a number string, e.g., "0123".
    .Print the original string and its data type.
    .Remove any leading zeros using .lstrip("0").
    .If the string becomes empty after removing zeros, replace it with "0".
    .Convert the cleaned string into an integer using int().
    .Print the cleaned number and its data type